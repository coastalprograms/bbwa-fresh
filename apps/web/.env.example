# Copy to .env.local and fill with your Supabase project details
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# Use either the publishable key (preferred) or the anon key
# If both are set, the publishable key will be used

# Server-only key for admin operations (never exposed to client)
SUPABASE_SERVICE_ROLE_KEY=

# AI provider configuration for Story 5.3
# Set AI_PROVIDER to either 'gemini' or 'openai' (defaults to 'gemini' if unset)
AI_PROVIDER=
GEMINI_API_KEY=
OPENAI_API_KEY=

# Google Maps API Key (client-side)
NEXT_PUBLIC_GOOGLE_MAPS_API_KEY=

# FAQ / Airtable configuration
# Set FAQ_SOURCE to 'airtable' to fetch from Airtable via the Supabase Edge Function wrapper,
# or 'local' to fetch from the Supabase 'faq' table (fallback/default).
FAQ_SOURCE=local
# Default number of items to render (cap per request)
FAQ_LIMIT=10

# Airtable credentials (Edge Functions only; do NOT set these in client or web app env)
# Leave empty here and configure as Supabase Function secrets instead:
#   AIRTABLE_API_KEY, AIRTABLE_BASE_ID, AIRTABLE_FAQ_TABLE, AIRTABLE_CONTACT_TABLE
# If you temporarily run server-only Airtable fetches from Next.js, keep these values in a
# non-checked-in local env file and ensure all code paths are server-only.
AIRTABLE_API_KEY=
AIRTABLE_BASE_ID=
AIRTABLE_TABLE=FAQ
# Optional view; if omitted, the default/table view will be used
AIRTABLE_VIEW=Grid view

# Field name overrides (optional; defaults shown)
AIRTABLE_FIELDS_QUESTION=Question
AIRTABLE_FIELDS_ANSWER=Answer
AIRTABLE_FIELDS_UPDATED=Last Modified

# Session secret for signing induction session cookies (server-only)
SESSION_SECRET=
